apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base
  - non-existent-resource.yaml  # This file doesn't exist - will cause build failure

patchesStrategicMerge:
  - invalid-patch.yaml

commonLabels:
  environment: dev